# 中国社会百态档案协作规范

本项目记录 1995 年以来具有长期观察价值的中国社会事件。目标不是罗列热搜，而是形成可追溯、可复核、可修订的月度公共档案。

## 不可妥协的原则

1. 不编造事件、日期、人物、引语、结论或链接。没有足够证据时保留在候选清单，不进入月度档案。
2. 明确区分已确认事实、当事方或机构的主张、媒体转述和编辑判断。
3. 涉及死亡、自杀、疾病、犯罪动机、网暴后果等敏感因果时，只有权威调查或可靠来源明确确认后才能写成因果关系；否则必须使用“发生于……之后”“尚无证据确认直接因果”等限定语。
4. “历史意义”是基于证据的解释，不是事实字段的替代品。避免预言式、煽情式和道德审判式表达。
5. 证据质量优先于每月数量。原则上每月 3–5 条；不足时在月文件的“本月说明”和 coverage.csv 的 notes 中记录原因，不得凑数。

## 收录标准

候选事件至少满足以下一项，并且有可复核的公开证据：

- 促成或体现法律、政策、行业规则、公共治理方式的变化；
- 揭示有代表性的社会结构、劳动关系、消费安全、教育医疗或数字生活问题；
- 引发持续性的公共讨论，且后续影响能够被可靠来源观察；
- 是理解某一时期社会观念或技术环境的重要切片。

单纯猎奇、只有匿名截图、无法核实的网传消息、纯娱乐营销以及重复事件原则上不收录。

## 来源要求

- 每条至少一个可直接访问的 HTTP(S) 新闻链接。优先使用政府、法院、监管机构等一手来源和具有编辑责任的新闻机构。
- 对争议事实、重大伤亡、违法定性或敏感因果，原则上需要两个相互独立的可靠来源；若只能取得一个来源，必须在描述中明确证据边界。
- 链接文字建议使用“机构或媒体｜标题｜发布日期”，便于链接失效后追溯。
- 社交平台帖子可以作为当事方陈述的证据，但不能单独承担事实核验。
- 不以搜索结果摘要、AI 回答、无法打开的转载页或没有出处的聚合页替代原始报道。

本地校验器只检查链接格式，不会证明网页真实、可访问或内容支持正文。人工复核仍是发布门槛。

## 月份归档规则

- 文件唯一合法路径为 archive/YYYY/YYYY-MM.md，年份目录必须与文件年月一致。
- 通常按事件首次形成公共事实或公共影响的月份归档。
- 跨月事件选择首次达到收录标准的月份，后续进展写入同一条目的描述，并注明日期；只有出现独立且重大的新阶段时才新建条目。
- 同一事件不得在多个月份重复复制。确需关联时使用文字交叉引用。
- 当前计划覆盖 1995-01 至 2026-08。coverage.csv 从 1995-01 起必须逐月连续；未来月度任务可在末尾顺序追加新月份。

## 固定记录结构

每条记录使用二级标题，从 01 连续编号，并包含以下四个非空字段：

1. 标题
2. 描述
3. 历史意义
4. 新闻链接

以 ENTRY_TEMPLATE.md 为唯一格式模板。新闻链接字段中的每一行都应为带说明文字的 Markdown HTTP(S) 链接。提交前删除“待补充”“TODO”“TBD”和示例域名等占位内容。

## coverage.csv 状态

coverage.csv 的字段固定为 month,status,event_count,verified_count,notes。

- pending：尚未开始；两个计数字段均为 0。
- seed_pending：已有候选种子但尚未迁移或核验；两个计数字段均为 0，notes 必须说明种子来源或待办。
- draft：已写入至少一条草稿。
- in_review：正在逐条核对正文与来源。
- verified：所有记录均已核验，verified_count 必须等于 event_count。

event_count 是月文件中的记录总数，verified_count 是已完成人工证据核验的记录数。任何增删记录都必须同步更新这两个字段和 notes。

## 推荐工作流

1. 在 coverage.csv 中选择一个 pending 或 seed_pending 月份，记录负责人或证据截止信息。
2. 建立候选清单，先去重，再核对事件发生月份。
3. 阅读原始来源和可靠报道，记录发布日期、核心事实与争议点。
4. 按模板写入月文件，避免从标题直接推导事实。
5. 逐条复核四字段、来源支持关系、敏感措辞和重复收录。
6. 同步 coverage.csv，并运行校验器。
7. 只有人工复核完成后才把状态改为 verified。

## 本地校验

在目标仓库根目录运行：

    python3 scripts/validate_archive.py --root . --format text

需要把每月 3–5 条作为硬门槛时运行：

    python3 scripts/validate_archive.py --root . --strict-count

机器可读结果：

    python3 scripts/validate_archive.py --root . --format json

退出码为 0 表示没有错误；1 表示发现结构或一致性错误。默认情况下，数量不在 3–5 条范围内会产生告警；启用 strict-count 后会变成错误。

## 修订与审计

- 修正事实时，在提交说明中写明原表述、修订依据和受影响月份，不静默改写争议事实。
- 删除事件时，在 coverage.csv 的 notes 中保留简短原因，并同步计数。
- 不把密钥、登录态、付费内容全文或个人敏感信息提交到仓库。
- 自动化可以创建候选或运行校验，但不能替代来源核验和发布决策。
